# Rusquitto — TODO

Open work only. Completed work → [progress.md](progress.md). Settled product
decisions (MQTT-5-only, no plugins, …) → [scope.md](scope.md).

Each item carries three badges — **priority** (value/severity), **risk**
(implementation risk), and **status** (state). Phases use task-list checkboxes.

---

*(Empty — the parked-connection idle path shipped in v2.0.0 and moved to
[progress.md](progress.md). Untracked possible follow-ups, none currently
scheduled: a SAN-based mTLS identity fallback when the cert has no CN; parser
fuzzing / property tests on the frame codec; wiring the crash-recovery and mTLS
harnesses plus the adversarial battery into CI.)*
